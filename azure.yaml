# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: MyBlog
services:  
  app:
    language: dotnet
    project: .\MyBlog.AppHost\MyBlog.AppHost.csproj
    host: containerapp
hooks:
  postprovision:
    posix:
      run: ./scripts/write-binding-to-azd-env.sh
      interactive: true
    windows:
      run: ./scripts/write-binding-to-azd-env.ps1
      interactive: true
